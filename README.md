# SafePass-Public
SafePass is a secure, offline-first password and document manager built with a zero-knowledge architecture. All data is encrypted locally using the master password as the sole root of trust. It works without internet, with optional encrypted cloud sync for multi-device access.
